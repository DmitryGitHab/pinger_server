"""Конфигурация и константы приложения.

Раньше эти значения были "магическими числами", разбросанными по main.py.
Вынесены сюда, чтобы менять поведение (таймауты, интервалы) в одном месте,
а в перспективе — читать их из переменных окружения (.env).
"""

DB_PATH = "ip_addresses.db"

# Таймаут одного ICMP-пинга, секунды
PING_TIMEOUT = 2

# Пауза между полными циклами пинга всех адресов, секунды
PING_INTERVAL = 1

# Как часто WebSocket отправляет клиенту свежие данные, секунды
WS_PUSH_INTERVAL = 5

# Валидация IPv4
IP_REGEX = r"^((25[0-5]|(2[0-4]|1\d|[1-9]|)\d)\.?\b){4}$"
